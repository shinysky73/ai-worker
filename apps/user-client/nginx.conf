server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    location /api {
        proxy_pass http://api-server:3002;
        proxy_http_version 1.1;
        client_max_body_size 50M;
    }
}
